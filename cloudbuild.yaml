
steps:
- name: 'ubuntu'
  args: ['sleep', '1']
- name: 'ubuntu'
  args: [ "echo", "That was a foo_bar withmessage! REPO_NAME ${REPO_NAME} COMMIT_SHA ${COMMIT_SHA}  REVISION_ID ${REVISION_ID}  SHORT_SHA ${SHORT_SHA}" ]
